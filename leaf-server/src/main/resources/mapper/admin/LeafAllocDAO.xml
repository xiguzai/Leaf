<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sankuai.inf.leaf.admin.dao.LeafAllocDAO">
  <resultMap id="LeafAllocDO" type="com.sankuai.inf.leaf.admin.model.LeafAllocDO">
    <id column="biz_tag" jdbcType="VARCHAR" property="bizTag" />
    <result column="max_id" jdbcType="BIGINT" property="maxId" />
    <result column="step" jdbcType="INTEGER" property="step" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    biz_tag, max_id, step, description, update_time
  </sql>
  <select id="listAll" parameterType="java.lang.String" resultMap="LeafAllocDO">
    select
    <include refid="Base_Column_List" />
    from leaf_alloc
  </select>
  <select id="countByBizTag" parameterType="java.lang.String" resultType="int">
    select
    count(*)
    from leaf_alloc
    where biz_tag = #{bizTag,jdbcType=VARCHAR}
  </select>
  <delete id="deleteByBizTag" parameterType="java.lang.String">
    delete from leaf_alloc
    where biz_tag = #{bizTag,jdbcType=VARCHAR}
  </delete>
  <insert id="insert" parameterType="com.sankuai.inf.leaf.admin.model.LeafAllocDO">
    insert into leaf_alloc (biz_tag, max_id, step, 
      description)
    values (#{bizTag,jdbcType=VARCHAR}, #{maxId,jdbcType=BIGINT}, #{step,jdbcType=INTEGER}, 
      #{description,jdbcType=VARCHAR})
  </insert>
  <update id="updateByBizTag" parameterType="com.sankuai.inf.leaf.admin.model.LeafAllocDO">
    update leaf_alloc
    set description = #{description,jdbcType=VARCHAR}
    where biz_tag = #{bizTag,jdbcType=VARCHAR}
  </update>
</mapper>